const IMG_OFFSET = 30;

const config = {
  $: {
    pikacute: {
      xOffset: -IMG_OFFSET,
      maxFrames: 24,
      color: "#edaa3d",
    },
    bulbasuck: {
      xOffset: IMG_OFFSET,
      maxFrames: 23,
      color: "#468849",
    },
  },
};
